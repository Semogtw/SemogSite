name: Refresh verified pnpm lockfile

on:
  push:
    branches:
      - develop/foundation-bootstrap
    paths:
      - .github/workflows/bootstrap-lockfile.yml

permissions:
  contents: write

concurrency:
  group: bootstrap-semogsite-lockfile
  cancel-in-progress: false

jobs:
  generate:
    name: Generate and commit deterministic lockfile
    runs-on: ubuntu-24.04
    timeout-minutes: 15

    steps:
      - name: Checkout branch
        uses: actions/checkout@v6
        with:
          ref: develop/foundation-bootstrap
          fetch-depth: 1

      - name: Set up Node 22
        uses: actions/setup-node@v6
        with:
          node-version: 22.23.1
          check-latest: false

      - name: Activate pinned pnpm
        shell: bash
        run: |
          set -euo pipefail
          corepack enable
          corepack prepare pnpm@10.14.0 --activate
          test "$(pnpm --version)" = "10.14.0"

      - name: Generate and verify lockfile
        shell: bash
        run: |
          set -euo pipefail
          pnpm install --no-frozen-lockfile --ignore-scripts
          printf '%s  %s\n' \
            'c6f6cb9ee7e073c64a92de6a0ffa4c61a9383ec5ac24d81584c530cd079a8671' \
            'pnpm-lock.yaml' | sha256sum -c -

      - name: Commit lockfile and remove bootstrap workflow
        shell: bash
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add pnpm-lock.yaml
          git rm .github/workflows/bootstrap-lockfile.yml
          git commit -m 'build: refresh deterministic pnpm lockfile'
          git push origin HEAD:develop/foundation-bootstrap
