name: Deprecated private workflow control gate

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  migrated:
    name: Use Offline-Toolchains public runner hub
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    steps:
      - name: Explain migration
        run: |
          echo 'This heavy private-repository gate has moved to Semogtw/Offline-Toolchains.'
          echo 'Run: Run private project CI -> project=semogsite.'
